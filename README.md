# DJANGO + DOCKER

## Repositório criado para configurar o Django utilizando Docker.

### Varíaveis alteradas no settings.py: SECRET_KEY, ALLOWED_HOSTS, DEBUG, DATABASES, DATA_DIR, LANGUAGE_CODE, TIME_ZONE, MEDIA_URL, MEDIA_ROOT
### Condição IF criada em urls.py